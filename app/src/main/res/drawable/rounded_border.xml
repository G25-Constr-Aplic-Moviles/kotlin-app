<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Fondo transparente -->
    <stroke
        android:width="2dp"
        android:color="#FF000000" /> <!-- Color y grosor del borde -->
    <corners android:radius="16dp" /> <!-- Borde redondeado -->
    <padding
        android:left="2dp"
        android:top="2dp"
        android:right="2dp"
        android:bottom="2dp" />
</shape>